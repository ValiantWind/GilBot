"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveColor = void 0;
const consts_1 = require("./consts");
/**
 * Copyright 2015 - 2021 Amish Shah
 * Copyrights licensed under the Apache License 2.0, https://github.com/discordjs/discord.js/blob/master/LICENSE
 * Taken from https://github.com/discordjs/discord.js/blob/stable/src/util/Util.js#L436
 */
function resolveColor(color) {
    var _a;
    if (typeof color === "string") {
        if (color === "RANDOM")
            return Math.floor(Math.random() * (0xffffff + 1));
        color = (_a = consts_1.COLORS[color.toUpperCase()]) !== null && _a !== void 0 ? _a : parseInt(color.replace("#", ""), 16);
    }
    else if (Array.isArray(color)) {
        color = (color[0] << 16) + (color[1] << 8) + color[2];
    }
    if (color < 0 || color > 0xffffff)
        throw new RangeError("COLOR_RANGE");
    else if (isNaN(color))
        throw new TypeError("COLOR_CONVERT");
    return color;
}
exports.resolveColor = resolveColor;
//# sourceMappingURL=util.js.map